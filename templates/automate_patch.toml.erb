[deployment]
  [deployment.v1.svc]
  channel = "<%= @channel %>"
    upgrade_strategy = "<%= @upgrade_strategy %>"
    deployment_type = "<%= @deployment_type %>"
    override_origin = "<%= @override_origin %>"
    hartifacts_path = "<%= @hartifacts_path %>"
